	#include<stdio.h>
	#include<conio.h>
	void main()
	{
	clrscr();
	int a,b;
	printf("\n enter any two numbers:");
	scanf("%d%d",&a,&b);
	printf("\na+b=%d is the required number",a+b);
	printf("\na-b=%d is the required number",a-b);
	printf("\na*b=%d is the required number",a*b);
	printf("\na/b=%d is the required number",a/b);
	getch();
	}